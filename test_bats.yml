version: '3.8'

services:
  bats:
    # image: bats/bats:latest
    build: ./test
    depends_on:
      - website
      - api
      - db
    volumes:
      - ./test/:/test/
    working_dir: /
    environment:
      BATS_baseURL: http://api/
    entrypoint: bats test/alfred.bats
